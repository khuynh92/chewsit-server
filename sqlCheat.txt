//Link to local database/create

CREATE DATABASE chews_it;

CREATE TABLE IF NOT EXISTS users (
id SERIAL PRIMARY KEY,
name VARCHAR(100) NOT NULL,
pin VARCHAR(20) 
);

CREATE TABLE IF NOT EXISTS favorites (
id SERIAL PRIMARY KEY,
yelp_id VARCHAR(255),
favorites_id INT REFERENCES users(id)
);

//test

INSERT INTO users (name , pin) VALUES ('Jesse', '8246');
INSERT INTO users (name, pin) VALUES ('Judah', '1234');
INSERT INTO favorites (yelp_id, favorites_id) values ('blah', 1);
INSERT INTO favorites (yelp_id, favorites_id) values ('this is more .blah1 address', 1);
INSERT INTO favorites (yelp_id, favorites_id) values ('morething.id842fromsomeoneelse', 2);
INSERT INTO FAVORITES (yelp_id, favorites_id) values ('thisplaceIhangout', 2);

 favorites (id, yelp_id) values (1, 'this stuff i like');

//get everything from both tables

app.get('/testUsers', (req, res) => {
  client.query(`
  SELECT * FROM users
  INNER JOIN favorites ON users.id = favorites.favorites_id;
  `)
    .then(results => res.send(results.rows));
});

//get only names from user table and yelp id from favorites table

app.get('/testUsers', (req, res) => {
  client.query(`
  SELECT users.name, favorites.yelp_id FROM users
  INNER JOIN favorites ON users.id = favorites.favorites_id
  WHERE users.id=2;
  `)
    .then(results => res.send(results.rows));
});

//database test new user
app.post('/addUser', (req, res) => {
  client.query(`
  INSERT INTO users (name, pin)
  VALUES ($1, $2);
  `,
  [request.params.name, request.params.pin]
  )
    .then(results => res.send(results.rows));
});

//database test new favorite
app.post('/newFavorite', (req, res) => {
  client.query(`
  INSERT INTO favorites (yelp_id, favorites_id)
  VALUES ($1, $2);
  `,
  [request.params.yelp_id, request.params.favorites_id]
  )
    .then(results => res.send(results.rows));
});

//database test update user info

//database test update favorite info




